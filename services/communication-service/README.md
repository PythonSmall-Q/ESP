# communication-service (占位)

技术栈: Node.js + TypeScript + Socket.IO + WebRTC
数据库: PostgreSQL (关系) + MongoDB (消息)
媒体服务器: Janus / LiveKit

端点示例：
- POST /api/v1/messages
- GET  /api/v1/channels/{id}/messages
- POST /api/v1/calls
- GET  /api/v1/users/online
