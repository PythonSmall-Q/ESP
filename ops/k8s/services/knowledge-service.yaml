apiVersion: apps/v1
kind: Deployment
metadata:
  name: knowledge-service
  namespace: efp-production
spec:
  replicas: 2
  selector:
    matchLabels:
      app: knowledge-service
  template:
    metadata:
      labels:
        app: knowledge-service
    spec:
      securityContext:
        runAsNonRoot: true
        fsGroup: 2000
      containers:
      - name: knowledge-service
        image: efp/knowledge-service:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 4005
        securityContext:
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: true
          runAsUser: 1000
          runAsGroup: 1000
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 500m
            memory: 512Mi
        readinessProbe:
          httpGet:
            path: /health
            port: 4005
          initialDelaySeconds: 10
        livenessProbe:
          httpGet:
            path: /health
            port: 4005
          initialDelaySeconds: 30
---
apiVersion: v1
kind: Service
metadata:
  name: knowledge-service
  namespace: efp-production
spec:
  selector:
    app: knowledge-service
  ports:
  - port: 4005
    targetPort: 4005
