apiVersion: apps/v1
kind: Deployment
metadata:
  name: selfservice-service
  namespace: efp-production
spec:
  replicas: 2
  selector:
    matchLabels:
      app: selfservice-service
  template:
    metadata:
      labels:
        app: selfservice-service
    spec:
      containers:
      - name: selfservice-service
        image: efp/selfservice-service:latest
        ports:
        - containerPort: 8080
        readinessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 10
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 30
---
apiVersion: v1
kind: Service
metadata:
  name: selfservice-service
  namespace: efp-production
spec:
  selector:
    app: selfservice-service
  ports:
  - port: 8080
    targetPort: 8080
