apiVersion: apps/v1
kind: Deployment
metadata:
  name: calendar-service
  namespace: efp-production
spec:
  replicas: 2
  selector:
    matchLabels:
      app: calendar-service
  template:
    metadata:
      labels:
        app: calendar-service
    spec:
      containers:
      - name: calendar-service
        image: efp/calendar-service:latest
        ports:
        - containerPort: 8080
        readinessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 10
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 30
---
apiVersion: v1
kind: Service
metadata:
  name: calendar-service
  namespace: efp-production
spec:
  selector:
    app: calendar-service
  ports:
  - port: 8080
    targetPort: 8080
