apiVersion: apps/v1
kind: Deployment
metadata:
  name: project-service
  namespace: efp-production
spec:
  replicas: 2
  selector:
    matchLabels:
      app: project-service
  template:
    metadata:
      labels:
        app: project-service
    spec:
      containers:
      - name: project-service
        image: efp/project-service:latest
        ports:
        - containerPort: 8080
        readinessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 10
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 30
---
apiVersion: v1
kind: Service
metadata:
  name: project-service
  namespace: efp-production
spec:
  selector:
    app: project-service
  ports:
  - port: 8080
    targetPort: 8080
