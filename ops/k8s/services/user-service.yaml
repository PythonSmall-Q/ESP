apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-service
  namespace: efp-production
spec:
  replicas: 2
  selector:
    matchLabels:
      app: user-service
  template:
    metadata:
      labels:
        app: user-service
    spec:
      containers:
      - name: user-service
        image: efp/user-service:latest
        ports:
        - containerPort: 8080
        readinessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 10
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 30
---
apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: efp-production
spec:
  selector:
    app: user-service
  ports:
  - port: 8080
    targetPort: 8080
