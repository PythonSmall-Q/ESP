apiVersion: apps/v1
kind: Deployment
metadata:
  name: booking-service
  namespace: efp-production
spec:
  replicas: 2
  selector:
    matchLabels:
      app: booking-service
  template:
    metadata:
      labels:
        app: booking-service
    spec:
      containers:
      - name: booking-service
        image: efp/booking-service:latest
        ports:
        - containerPort: 8080
        readinessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 10
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 30
---
apiVersion: v1
kind: Service
metadata:
  name: booking-service
  namespace: efp-production
spec:
  selector:
    app: booking-service
  ports:
  - port: 8080
    targetPort: 8080
