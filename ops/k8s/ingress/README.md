Ingress resources for exposing services via NGINX Ingress Controller.

Prerequisites:
- NGINX Ingress Controller installed in the cluster.
- DNS pointing to the ingress controller load balancer.

Files:
- One ingress per service under this folder.
